# Changelog

## 0.1.1 — 2026-09-25

**修复：会话列表不刷新（顺序不动、新会话不出现、刷新按钮看起来没用）**

增量更新时 `syncSessionNode` 调了一个没导出到 `window` 的内部函数，每次都抛
`ReferenceError`，循环中断在第一个会话 —— 后面的排序和「插入新会话」根本执行不到。
首屏走整页渲染，不经过这条路，所以只有面板已经开着时才发作。表现为时间文案会变、
顺序永远不变、新会话永远不出现。

- 导出该函数并改为显式 `window.` 调用
- 新增契约测试：`views.js` 的内部函数，`main.js` 不得裸调（两种失效方向都验过会挂）
- 新增两个诊断页：`dev/reorder-test.html`（复现这次的 bug）、`dev/serve.mjs`

**改动：会话预览抽屉**

- 改成回答「聊到哪了」：**最后一次提问 + 最后一条回复**（原来只给首条提问 + 三条被截断的回复）
- 最后回复不再限制 3 行，也不再截到 300 字（上限 900 字），能读完
- 新增 Markdown 清洗：标题符号、加粗、表格竖线、代码围栏等不再原样铺在窄抽屉里
- 首条提问压成底部一行小字；只有一次提问的会话不重复显示

## 0.1.0 — 2026-09-24

首个版本。

**面板**
- 活动栏底部入口，侧边栏面板
- 项目区：看板 / 列表两种视图，状态以颜色脊编码
- 会话区：按时间排列，节点大小 = 提问数
- 「工作脉搏」：最近 8 周提问数条带，空周保留基线
- 会话预览抽屉：首条提问 + 最后三条回复
- 跟随 VS Code 主题自动切换明暗，不写死颜色

**与 Claude Code 集成**
- 点击会话可在 Claude Code 扩展中恢复（命令优先，官方深链兜底）
- 未安装 Claude Code 扩展时降级为「只能预览」，不报错

**数据**
- 项目：扫描配置的目录，状态从目录名后缀解析（中英文状态词都认）
- 会话：流式解析 `~/.claude/projects/*/*.jsonl`，24MB 文件不爆内存
- 提问计数排除系统噪音（后台任务通知、斜杠命令），并正确处理
  带标签前缀的真实提问
- 会话目录用 jsonl 内的 `cwd` 字段反查，不依赖目录命名规则

**界面**
- 中英双语，跟随 VS Code 显示语言
- 可选液态玻璃质感（`taskboard.glassEffect`），深色主题下默认开启
